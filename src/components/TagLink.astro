---
import EmojiIcon from "./EmojiIcon.astro";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
	tag: string;
}

const { tag, href, class: className, ...rest } = Astro.props;
---
<a href={href} class:list={[className, 'tagLink']} {...rest}>
  <div class="tagIconArea">
    <EmojiIcon>🏷️</EmojiIcon>
  </div>
  <span>{tag}</span>
</a>
<style>
  .tagLink {
    display: inline-flex;
    color: var(--font-color-medium-emphasis);
  }
  .tagLink:hover {
    text-decoration: underline;
  }
  .tagIconArea {
    width: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }
</style>
